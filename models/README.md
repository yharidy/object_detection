# models/

This directory is for storing trained model checkpoints and weights.

## Structure

- `checkpoints/`: Training checkpoints saved during model training.
- `pretrained/`: Pretrained model weights for transfer learning.