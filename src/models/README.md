# models/

This directory contains neural network architectures and model definitions for object detection.

## Contents

Currently empty. Add model implementations here, such as:

- Backbone networks (ResNet, EfficientNet, etc.)
- Detection heads (SSD, YOLO, RetinaNet, etc.)
- Custom architectures for 2D/3D detection